/**
 * Generated TripServices API types live here.
 *
 * Run `npm run gen:api` (openapi-typescript) against the TripServices OpenAPI spec
 * to emit `tripservices.ts`, then re-export the operations / component schemas you
 * need (prefixed per CLAUDE.md, e.g. `TripServicesTrip`). Do not hand-write API
 * types — generate them.
 */

export {}
