# Trip display

Status: **route placeholder**. The core PNR / Order / Trip workspace.

- **Route:** `/trips`
- **TripServices endpoints (planned):** `GET /trips`, `GET /trips/{id}`, `PATCH /trips/{id}`
- **Known limitations:** placeholder page only; no data fetching yet.
