# Profiles

Status: **route placeholder**. Stored traveller preferences and document data.

- **Route:** `/profiles`
- **TripServices endpoints (planned):** `GET /profiles`, `GET /profiles/{id}`
- **Known limitations:** placeholder page only; no data fetching yet.
