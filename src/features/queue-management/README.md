# Queue management

Status: **route placeholder**. The daily operational hub — queue list, single-queue
view, bulk actions, and inline Productivity Automator exceptions.

- **Route:** `/queues`
- **TripServices endpoints (planned):** `GET /queues`, `GET /queues/{id}/entries`, `POST /queue-place`
- **Known limitations:** placeholder page only; no data fetching yet.
